# Version Timeline Demo - 使用指南

## 📦 快速开始

### 1. 安装依赖

```bash
cd version-demo
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

应用将在 `http://localhost:5173` 启动

### 3. 构建生产版本

```bash
npm run build
```

### 4. 预览生产构建

```bash
npm run preview
```

## 🎨 设计亮点

### 遵循 UIAgentRules.md 规范

1. **色彩系统**
   - ✅ 使用语义化颜色（`primary`, `secondary`, `muted` 等）
   - ✅ 支持深色模式
   - ✅ 紫色系仅用于小面积点缀

2. **图标系统**
   - ✅ 统一使用 Lucide React 图标库
   - ✅ 图标大小与文字协调

3. **微交互**
   - ✅ 所有按钮包含 hover/active 状态
   - ✅ 过渡动画 150-200ms
   - ✅ 激活节点带波纹效果（ripple animation）

4. **布局标准**
   - ✅ 使用 Tailwind 间距系统
   - ✅ 响应式布局（移动端友好）
   - ✅ 圆角统一使用 `rounded-lg/xl/2xl`

## 🧩 组件架构

```
src/
├── components/
│   ├── VersionCard.tsx       # 版本卡片
│   ├── TimelineNode.tsx      # 时间线节点（带波纹动效）
│   ├── TagChip.tsx           # 标签芯片
│   ├── PromptCard.tsx        # 提示卡片
│   ├── SearchBox.tsx         # 搜索框
│   ├── AddButton.tsx         # 添加按钮
│   ├── VersionTimeline.tsx   # 主时间线组件
│   └── SidePanel.tsx         # 侧边栏面板
├── data/
│   └── mockData.ts           # 模拟数据
├── types/
│   └── index.ts              # 类型定义
├── lib/
│   └── utils.ts              # 工具函数（cn）
├── App.tsx                   # 主应用
├── main.tsx                  # 入口文件
└── index.css                 # 全局样式（Tailwind + CSS Variables）
```

## 🎯 核心特性

### 1. 版本时间线
- 左右交替布局
- 中心垂直主轴
- 节点连接线
- 鼠标悬停高亮激活
- 拖拽手柄（悬停显示）

### 2. 子版本支持
- 缩进展示
- 左侧分支线
- 圆点标记

### 3. 侧边面板
- 搜索功能（实时过滤）
- 标签过滤（多选）
- 提示卡片列表

### 4. 响应式设计
- 桌面端：时间线 + 侧边栏
- 移动端：垂直堆叠

## 🎨 主题定制

在 `tailwind.config.js` 中修改主题变量：

```javascript
theme: {
  extend: {
    colors: {
      primary: "hsl(var(--primary))",
      secondary: "hsl(var(--secondary))",
      // ...
    }
  }
}
```

在 `src/index.css` 中修改 CSS 变量：

```css
:root {
  --primary: 262 83% 58%;    /* 紫色主色 */
  --secondary: 262 90% 70%;  /* 次要色 */
  /* ... */
}
```

## 🚀 性能优化

1. **懒加载**：可按需添加 `React.lazy()` 分割组件
2. **虚拟滚动**：大量版本时可使用 `react-window`
3. **记忆化**：使用 `React.memo()` 优化渲染
4. **动画性能**：使用 CSS transforms（已实现）

## 📱 浏览器兼容

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 🛠️ 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2 | UI 框架 |
| TypeScript | 5.2 | 类型安全 |
| Vite | 5.0 | 构建工具 |
| Tailwind CSS | 3.3 | 样式系统 |
| Lucide React | 0.294 | 图标库 |
| clsx + tailwind-merge | - | 类名合并 |

## 📝 开发建议

1. **添加新组件**：在 `src/components/` 创建，并导出到 `index.ts`
2. **修改数据**：编辑 `src/data/mockData.ts`
3. **添加类型**：在 `src/types/index.ts` 定义
4. **样式调整**：优先使用 Tailwind 类名，避免自定义 CSS

## 🎯 下一步优化

- [ ] 添加拖拽排序功能（react-dnd）
- [ ] 添加版本比对功能
- [ ] 添加深色模式切换按钮
- [ ] 添加搜索高亮
- [ ] 添加键盘快捷键
- [ ] 添加导出功能（JSON/PDF）
- [ ] 添加撤销/重做功能
- [ ] 添加协作编辑功能

## 📖 参考文档

- [UIAgentRules.md](../.github/UIAgentRules.md) - 项目设计规范
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Lucide Icons](https://lucide.dev/)
- [React 文档](https://react.dev/)
